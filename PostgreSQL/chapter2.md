# 📂 Глава 2: Организация данных на диске — от кластера до страницы

**Что вы узнаете:**
- Как PostgreSQL организует дисковое пространство: от директории `PGDATA` до отдельной страницы.
- Что такое OID, `relfilenode`, FSM и VM — и как они связаны с файлами на диске.
- Как устроена страница (блок) изнутри: заголовок, указатели, кортежи.
- Как устроен кортеж (строка): `t_xmin`, `t_xmax`, `t_ctid` и hint bits.
- Как работает TOAST и почему большие значения не ломают страницу.
- Как найти свою таблицу на диске и заглянуть внутрь страницы.
- Что `shared_buffers` кэширует **все** страницы: таблиц, индексов, FSM и VM.

**После прочтения вы сможете:**
- Объяснить полную иерархию хранения: кластер → база → схема → таблица → файл → страница → кортеж.
- Найти физические файлы своей таблицы через SQL-запросы.
- Понять, почему `UPDATE` создаёт новую версию строки, а не перезаписывает старую.
- Объяснить, зачем нужны FSM и VM, и что будет, если они устареют.
- Диагностировать раздувание таблиц и понимать, как TOAST влияет на производительность.

---

## Содержание

- [2.0 Пролог: где физически лежит твоя строка?](#20-пролог-где-физически-лежит-твоя-строка)
- [2.1 Иерархия дискового пространства](#21-иерархия-дискового-пространства)
- [2.2 Уровень 1: Кластер баз данных](#22-уровень-1-кластер-баз-данных)
- [2.3 Уровень 2: База данных](#23-уровень-2-база-данных)
- [2.4 Уровень 3: Схема](#24-уровень-3-схема)
- [2.5 Уровень 4 и 5: Таблицы, индексы и их файлы](#25-уровень-4-и-5-таблицы-индексы-и-их-файлы)
- [2.6 Уровень 6: Как файл разбивается на страницы](#26-уровень-6-как-файл-разбивается-на-страницы)
- [2.7 Практический пример: находим свою таблицу на диске](#27-практический-пример-находим-свою-таблицу-на-диске)
- [2.8 Выбор размера блока](#28-выбор-размера-блока)
- [2.9 Анатомия страницы](#29-анатомия-страницы)
- [2.10 Заголовок страницы](#210-заголовок-страницы)
- [2.11 Массив указателей ItemId](#211-массив-указателей-itemid)
- [2.12 Кортеж: структура строки](#212-кортеж-структура-строки)
- [2.13 Заголовок кортежа](#213-заголовок-кортежа)
- [2.14 Цепочка версий при UPDATE](#214-цепочка-версий-при-update)
- [2.15 Свободное пространство и FSM](#215-свободное-пространство-и-fsm)
- [2.16 TOAST: когда строка не влезает](#216-toast-когда-строка-не-влезает)
- [2.17 Аннотации сложности](#217-аннотации-сложности)
- [2.18 Практика Go: pageinspect](#218-практика-go-pageinspect)
- [2.19 Выводы и типичные ошибки](#219-выводы-и-типичные-ошибки)
- [2.20 Для быстрого повторения](#220-для-быстрого-повторения)
- [2.21 Вопросы для самопроверки](#221-вопросы-для-самопроверки)
- [2.22 Ответы](#222-ответы)
- [2.23 Куда идти дальше?](#223-куда-идти-дальше)

---

## 2.0 Пролог: где физически лежит твоя строка?

В Главе 1 мы разобрали, как Backend-процесс выполняет `UPDATE users SET name = 'Alice' WHERE id = 1`. Мы выяснили, что:

- Backend-процесс через Buffer Manager запрашивает страницу с данными. Если страницы нет в `shared_buffers` — читает её с диска.
- Изменённая страница становится «грязной» и позже сбрасывается на диск.
- Параллельно изменение записывается в WAL-буфер, откуда WAL Writer сбрасывает его в файлы `pg_wal/`.

Но мы **не ответили на главный вопрос**: где физически лежит эта страница на диске? Как PostgreSQL организует дисковое пространство? И как мы от базы данных приходим к конкретной странице?

❓ **Почему нельзя просто хранить все данные в одном файле?**

Представь файл на 1 ТБ, в котором лежат все таблицы, индексы и системные каталоги. Чтобы найти строку в таблице `users`, нужно либо просканировать весь этот гигантский файл, либо иметь сложную систему смещений. При удалении таблицы пришлось бы «дырявить» файл. При сбое риск потерять вообще всё.

💡 Решение: **иерархическая организация дискового пространства**. Данные организованы в виде строгой иерархии: кластер → базы данных → схемы → таблицы и индексы → файлы → страницы. Каждый уровень имеет чёткие границы и управляется независимо.

---

## 2.1 Иерархия дискового пространства

Прежде чем погружаться в страницы, нужно понять, как PostgreSQL видит дисковое пространство. Вот полная иерархия — от самого верхнего уровня до конкретной страницы:

```
УРОВЕНЬ 1: КЛАСТЕР БАЗ ДАННЫХ (Database Cluster)
├── Инициализируется командой initdb
├── Это одна директория на диске (PGDATA)
├── Содержит ВСЕ базы данных этого экземпляра PostgreSQL
├── Именно здесь лежат конфигурационные файлы (postgresql.conf, pg_hba.conf)
│
├── УРОВЕНЬ 2: БАЗА ДАННЫХ (Database)
│   ├── Создаётся командой CREATE DATABASE
│   ├── Физически: поддиректория внутри PGDATA/base/
│   ├── Идентифицируется по OID (Object Identifier)
│   ├── Каждая база изолирована: нельзя сделать JOIN между таблицами разных баз
│   │
│   ├── УРОВЕНЬ 3: СХЕМА (Schema)
│   │   ├── Создаётся командой CREATE SCHEMA
│   │   ├── Это ЛОГИЧЕСКОЕ пространство имён, а не физическая директория
│   │   ├── Схема public создаётся по умолчанию в каждой базе
│   │   ├── В одной базе может быть много схем
│   │   ├── Схемы позволяют организовать таблицы в логические группы
│   │   │
│   │   ├── УРОВЕНЬ 4: ТАБЛИЦЫ, ИНДЕКСЫ, МАТЕРИАЛИЗОВАННЫЕ ПРЕДСТАВЛЕНИЯ
│   │   │   ├── Создаются командами CREATE TABLE, CREATE INDEX и т.д.
│   │   │   ├── Каждый объект имеет свой OID
│   │   │   ├── Физически: ОДИН или НЕСКОЛЬКО файлов на диске
│   │   │   ├── Каждый файл не превышает 1 ГБ (если таблица больше — создаётся новый файл)
│   │   │   │
│   │   │   ├── УРОВЕНЬ 5: ФАЙЛЫ ТАБЛИЦЫ (Relation Files)
│   │   │   │   ├── Основной файл: 16385 (relfilenode)
│   │   │   │   ├── Карта свободного места: 16385_fsm
│   │   │   │   ├── Карта видимости: 16385_vm
│   │   │   │   ├── Если таблица > 1 ГБ: 16385.1, 16385.2, ...
│   │   │   │   │
│   │   │   │   ├── УРОВЕНЬ 6: СТРАНИЦЫ (Pages / Blocks)
│   │   │   │   │   ├── Размер по умолчанию: 8 КБ
│   │   │   │   │   ├── Каждый файл состоит из множества страниц
│   │   │   │   │   ├── Страница — минимальная единица чтения/записи
│   │   │   │   │   └── Внутри страницы: заголовок, массив указателей, кортежи
```

---

## 2.2 Уровень 1: Кластер баз данных

**Кластер баз данных** (database cluster) — это **самый верхний уровень**. Это одна директория на диске, которая создаётся командой `initdb` и содержит **все** базы данных одного экземпляра PostgreSQL.

Термин «кластер» здесь не имеет отношения к кластеризации серверов (как в Patroni). Это логическое понятие: «набор баз данных, управляемых одним процессом postmaster».

```
/var/lib/postgresql/data/          ← PGDATA (корень кластера)
├── postgresql.conf                ← основной конфигурационный файл
├── pg_hba.conf                    ← настройки аутентификации
├── PG_VERSION                     ← версия PostgreSQL
├── base/                          ← ВСЕ базы данных (самое важное!)
├── pg_wal/                        ← WAL-файлы (журнал предзаписи)
├── pg_tblspc/                     ← символические ссылки на tablespace
├── pg_stat/                       ← статистика
├── pg_logical/                    ← слоты логической репликации
└── global/                        ← общие системные каталоги (pg_database, pg_authid)
```

**Почему это важно:** на одном сервере может работать **только один** экземпляр PostgreSQL с одним кластером. Если нужно несколько изолированных наборов баз, запускают несколько экземпляров PostgreSQL на разных портах с разными `PGDATA`.

---

## 2.3 Уровень 2: База данных

Когда ты выполняешь `CREATE DATABASE mydb`, PostgreSQL создаёт **поддиректорию** внутри `PGDATA/base/`. Имя этой поддиректории — **OID базы данных** (числовой идентификатор).

```
/var/lib/postgresql/data/base/
├── 1/          ← системная база template1
├── 4/          ← системная база template0
├── 16384/      ← твоя база mydb (OID = 16384)
└── 16385/      ← другая база (OID = 16385)
```

Узнать OID своей базы можно запросом:

```sql
SELECT oid, datname FROM pg_database;
-- oid=16384, datname='mydb'
```

**Изоляция:** базы данных в PostgreSQL **полностью изолированы** друг от друга. Нельзя сделать JOIN между таблицами из разных баз. Нельзя использовать одну транзакцию для изменения данных в двух базах. Для запросов между базами используют Foreign Data Wrappers (FDW).

---

## 2.4 Уровень 3: Схема

**Схема** (schema) — это **логическое пространство имён** внутри базы данных. В отличие от баз данных, схемы **не имеют физического представления на диске** — нет директории с именем схемы.

Схемы создаются внутри базы данных:

```sql
CREATE SCHEMA accounting;     -- схема для бухгалтерии
CREATE SCHEMA warehouse;      -- схема для склада

-- Теперь можно создавать таблицы в разных схемах:
CREATE TABLE accounting.invoices (...);
CREATE TABLE warehouse.products (...);
```

### Как PostgreSQL управляет схемами

Схемы хранятся в системном каталоге `pg_namespace` — это обычная таблица внутри базы данных, которая содержит информацию о всех схемах:

```sql
SELECT oid, nspname FROM pg_namespace;
-- oid=11, nspname='pg_catalog'    (системные таблицы)
-- oid=2200, nspname='public'      (схема по умолчанию)
-- oid=16384, nspname='accounting' (пользовательская схема)
-- oid=16385, nspname='warehouse'  (пользовательская схема)
```

Каждая таблица в `pg_class` имеет поле `relnamespace`, которое ссылается на OID схемы из `pg_namespace`. Именно так PostgreSQL понимает, к какой схеме принадлежит таблица.

**Механизм `search_path`** определяет, в каких схемах искать таблицы:

```sql
SHOW search_path;
-- "$user", public

-- Это означает: сначала ищем в схеме с именем текущего пользователя,
-- потом в схеме public
```

Когда ты пишешь `SELECT * FROM users`, PostgreSQL:
1. Смотрит `search_path` — получает список схем для поиска.
2. Идёт в `pg_namespace`, находит OID'ы этих схем.
3. Идёт в `pg_class`, ищет таблицу с именем `users`, у которой `relnamespace` равен OID'у одной из схем.
4. Если таблица найдена — получает её `relfilenode` и идёт читать файлы.

**Зачем нужны схемы:**
- Логическая группировка таблиц (по отделам, по модулям приложения).
- Управление правами доступа: можно дать доступ к схеме `accounting` только бухгалтерам.
- Изоляция: можно иметь таблицы с одинаковыми именами в разных схемах (`accounting.orders` и `warehouse.orders`).

---

## 2.5 Уровень 4 и 5: Таблицы, индексы и их файлы

Когда ты создаёшь таблицу, PostgreSQL назначает ей **уникальный OID** (Object Identifier) — 32-битное число, которое идентифицирует объект внутри кластера. OID'ы используются для всех объектов: таблиц, индексов, схем, функций, представлений.

Для таблиц и индексов есть ещё одно важное число — **relfilenode** (relation file node). Это **имя файла** на диске. Обычно `relfilenode` совпадает с OID, но может измениться после операций вроде `VACUUM FULL`, `CLUSTER` или `TRUNCATE`.

### Что такое 16385, 16385_fsm и 16385_vm

Когда ты видишь файлы с такими именами в директории базы данных:

```
/var/lib/postgresql/data/base/16384/
    ├── 16385              ← основной файл таблицы (САМИ ДАННЫЕ)
    ├── 16385_fsm          ← Free Space Map (карта свободного места)
    └── 16385_vm           ← Visibility Map (карта видимости)
```

- **16385** — это `relfilenode`. Основной файл, содержащий **все страницы** с данными таблицы. Если таблица больше 1 ГБ, создаются дополнительные файлы: `16385.1`, `16385.2` и т.д.

- **16385_fsm** — **Free Space Map**. Вспомогательный файл, который хранит информацию о том, сколько свободного места есть на каждой странице. Когда нужно вставить новую строку, PostgreSQL смотрит в FSM и быстро находит страницу с достаточным местом — без сканирования всей таблицы.

- **16385_vm** — **Visibility Map**. Вспомогательный файл, который хранит битовую карту: для каждой страницы отмечено, все ли строки на ней видны всем транзакциям. Это нужно для Index Only Scan: если все строки на странице видны всем, можно не читать саму таблицу.

Все эти файлы привязаны к `relfilenode`. Узнать его можно запросом:

```sql
SELECT relname, relfilenode, oid, relpages, reltuples
FROM pg_class 
WHERE relname = 'users';
-- relname='users', relfilenode=16385, oid=16385, relpages=12800, reltuples=1000000
```

**Важный момент про индексы:** у индекса **свой собственный** `relfilenode`, и его файлы лежат **в той же директории** базы данных. Если у таблицы `users` есть индекс по `id`, то рядом с файлом `16385` будет лежать файл индекса — например, `16386`. Индекс — это отдельный объект со своими страницами, своим FSM и своим VM.

### 💡 Практика: как найти файлы своей таблицы на диске

**✅ ОБЯЗАТЕЛЬНО ДЕЛАЙ:**

- **Умей находить таблицу через `pg_class`:**
  ```sql
  -- 1. OID базы
  SELECT oid FROM pg_database WHERE datname = 'mydb';
  -- oid = 16384

  -- 2. relfilenode таблицы
  SELECT relfilenode FROM pg_class WHERE relname = 'users';
  -- relfilenode = 16385

  -- 3. Файлы лежат в /var/lib/postgresql/data/base/16384/16385*
  ```

**👍 СТОИТ СДЕЛАТЬ:**

- **Знать, где лежат `_fsm` и `_vm`** — они критичны для Index Only Scan (Глава 3).

**❌ НЕ ДЕЛАЙ:**

- **Не трогай файлы руками.** PostgreSQL сам управляет ими. Ручное изменение файлов может разрушить базу.

---

## 2.6 Уровень 6: Как файл разбивается на страницы

Теперь самый важный вопрос: **как файл таблицы разбивается на страницы?**

### Физическое разбиение: страницы идут последовательно

Файл таблицы **физически** разбит на страницы фиксированного размера. Это не логическое деление (как схемы), а **реальное физическое разбиение** на диске. Каждая страница занимает ровно `BLCKSZ` байт (по умолчанию 8192 байта = 8 КБ), и они идут друг за другом без зазоров:

```
ФАЙЛ ТАБЛИЦЫ (16385) НА ДИСКЕ:
┌──────────────────────────────────────────────────────────────────┐
│                          ФАЙЛ НА ДИСКЕ                            │
│  (последовательность байт, без разрывов)                          │
│                                                                  │
│  Байты 0–8191         Байты 8192–16383      Байты 16384–24575   │
│  ┌────────────────┐   ┌────────────────┐   ┌────────────────┐   │
│  │                │   │                │   │                │   │
│  │  СТРАНИЦА 0    │   │  СТРАНИЦА 1    │   │  СТРАНИЦА 2    │   │
│  │  (8 КБ)        │   │  (8 КБ)        │   │  (8 КБ)        │   │
│  │                │   │                │   │                │   │
│  └────────────────┘   └────────────────┘   └────────────────┘   │
│                                                                  │
│  ... и так до конца файла ...                                    │
│                                                                  │
│  Байты (N-1)*8192 – N*8192-1                                     │
│  ┌────────────────┐                                              │
│  │                │                                              │
│  │  СТРАНИЦА N    │                                              │
│  │  (8 КБ)        │                                              │
│  │                │                                              │
│  └────────────────┘                                              │
│                                                                  │
│  ВАЖНО: каждая страница начинается с границы, кратной 8192.      │
│  Страница 0 → смещение 0                                         │
│  Страница 1 → смещение 8192                                      │
│  Страница 2 → смещение 16384                                     │
│  Страница N → смещение N * 8192                                  │
└──────────────────────────────────────────────────────────────────┘
```

### Как PostgreSQL находит страницу в файле

Когда Buffer Manager'у нужна страница №100, он вычисляет смещение в файле:

```
смещение = номер_страницы * размер_страницы
         = 100 * 8192
         = 819200 байт
```

И выполняет системный вызов `pread()` для чтения 8192 байт начиная со смещения 819200. Это одна операция ввода-вывода. После этого страница загружается в `shared_buffers`.

### ВАЖНО: shared_buffers кэширует ВСЕ страницы, а не только таблицы

Когда мы говорим «страница», мы имеем в виду **не только страницы таблиц**. В `shared_buffers` лежат:

- **Страницы таблиц** (Heap Pages)
- **Страницы индексов** (B-tree Pages, GIN Pages, GiST Pages...)
- **Страницы FSM** (Free Space Map)
- **Страницы VM** (Visibility Map)

Buffer Manager **не различает** их: для него это просто «страница с номером X в файле Y». Всё, что читается с диска, проходит через `shared_buffers`. Когда в Главе 3 мы будем говорить про поиск по индексу, помни: страницы индексов загружаются в `shared_buffers` **тем же самым механизмом**, что и страницы таблиц.

### Что происходит, когда таблица растёт

Когда создаётся новая страница (например, при вставке, когда все существующие страницы заполнены), PostgreSQL просто дописывает 8192 байт в конец файла. Файл растёт скачками по 8 КБ.

Если файл достигает 1 ГБ, PostgreSQL **автоматически создаёт новый файл** с суффиксом `.1`:

```
16385        ← 0 – 1 ГБ (страницы 0 – 131071)
16385.1      ← 1 ГБ – 2 ГБ (страницы 131072 – 262143)
16385.2      ← 2 ГБ – 3 ГБ (страницы 262144 – 393215)
```

Страницы внутри каждого файла нумеруются **с нуля относительно начала этого файла**, но логически они продолжают общую нумерацию. Страница 131072 — это страница 0 в файле `16385.1`.

### Визуализация: как таблица растёт на диске

```
ВРЕМЯ →
──────────────────────────────────────────────────────────────────→

t=0: Только что создали таблицу CREATE TABLE users (...)
  Файл: 16385 (0 байт — пустой)

t=1: Вставили первые строки
  Файл: 16385 (8 КБ = 1 страница)
  ┌────────┐
  │ Стр. 0 │
  └────────┘

t=2: Вставили ещё строки, первая страница заполнилась
  Файл: 16385 (16 КБ = 2 страницы)
  ┌────────┬────────┐
  │ Стр. 0 │ Стр. 1 │
  └────────┴────────┘

t=3: Таблица выросла до 1 ГБ
  Файл: 16385 (1 ГБ = 131072 страницы)
  ┌────────┬────────┬────────┬─────┬────────────┐
  │ Стр. 0 │ Стр. 1 │ Стр. 2 │ ... │ Стр. 131071│
  └────────┴────────┴────────┴─────┴────────────┘

t=4: Таблица превысила 1 ГБ
  Файлы:
  16385   (1 ГБ) ┌────────┬─────┬────────────┐
                 │ Стр. 0 │ ... │ Стр. 131071│
                 └────────┴─────┴────────────┘
  16385.1 (8 КБ) ┌────────────┐
                 │ Стр. 131072│  ← логически продолжает нумерацию
                 └────────────┘
```

---

## 2.7 Практический пример: находим свою таблицу на диске

Допустим, ты хочешь **физически** найти файлы своей таблицы на диске:

```sql
-- 1. Узнаём OID базы данных
SELECT oid, datname FROM pg_database WHERE datname = 'mydb';
-- oid = 16384

-- 2. Узнаём relfilenode таблицы
SELECT relfilenode, relname, relpages, reltuples 
FROM pg_class WHERE relname = 'users';
-- relfilenode = 16385, relpages = 12800, reltuples = 1000000

-- 3. Вычисляем размер таблицы
-- 12800 страниц * 8 КБ = 102 400 КБ ≈ 100 МБ

-- 4. Идём на сервер и находим файлы:
-- cd /var/lib/postgresql/data/base/16384/
-- ls -la 16385*
-- -rw------- 1 postgres postgres 104857600 Jan 1 00:00 16385
-- -rw------- 1 postgres postgres     24576 Jan 1 00:00 16385_fsm
-- -rw------- 1 postgres postgres      8192 Jan 1 00:00 16385_vm
```

Ты только что нашёл физические файлы, в которых лежат данные таблицы `users`. Именно эти файлы читает и изменяет Buffer Manager, когда ты делаешь `SELECT` или `UPDATE`. Страница №100 лежит по смещению 100 × 8192 = 819200 байт в файле `16385`.

---

## 2.8 Выбор размера блока

Размер блока выбирается **один раз при инициализации всего кластера баз данных** (при `initdb`) и не может быть изменён потом для отдельной базы или таблицы.

По умолчанию блок равен 8 КБ, но PostgreSQL можно собрать из исходников с другим значением `BLCKSZ` — степенью двойки от 1 до 32 КБ:

```
+-----------------------------------------------------------+
| ВОЗМОЖНЫЕ РАЗМЕРЫ БЛОКА (степень двойки, от 1 до 32 КБ)   |
+-----------------------------------------------------------+
|  1 КБ  |  Не используется, слишком мал                   |
|  2 КБ  |  Теоретически возможен                          |
|  4 КБ  |  Экзотика для систем с мелкими записями         |
|  8 КБ  |  СТАНДАРТНЫЙ РАЗМЕР ПО УМОЛЧАНИЮ               |
| 16 КБ  |  OLAP-системы, меньше накладных расходов        |
| 32 КБ  |  МАКСИМАЛЬНЫЙ РАЗМЕР (тяжёлый OLAP)            |
+-----------------------------------------------------------+
```

**Зачем менять размер блока?**
- **32 КБ**: используется в OLAP-системах (аналитических), где много последовательного чтения больших объёмов данных. Чем больше блок, тем меньше накладных расходов на чтение множества мелких блоков с диска.
- **4 КБ**: экзотика для систем с очень большим количеством случайных точечных записей. Меньший блок уменьшает «оказываемое воздействие» при записи (WAL-записи могут быть меньше), но увеличивает фрагментацию.

**Жёсткое следствие:** максимальная строка не может превышать примерно 1/4 блока. Для 8 КБ это ~2 КБ, для 32 КБ — ~8 КБ. Всё, что больше, уходит в TOAST.

### 💡 Практика: какой BLCKSZ выбирать

**✅ ОБЯЗАТЕЛЬНО ДЕЛАЙ:**

- **Знай: дефолт 8 КБ, меняется только при `initdb`.** На лету изменить нельзя — только пересоздать кластер.

**🤔 НЕ ОБЯЗАТЕЛЬНО:**

- **Менять на 32 КБ для OLAP на PostgreSQL.** Выгода минимальна, потому что PostgreSQL остаётся row-based. Для серьёзной аналитики лучше смотреть в сторону ClickHouse или TimescaleDB (обсудим в Приложении D).

**❌ НЕ ДЕЛАЙ:**

- **Не пытайся изменить BLCKSZ на лету.** Это невозможно. Если очень нужно — `pg_dump` → новый кластер с другим BLCKSZ → `pg_restore`.

---

## 2.9 Анатомия страницы

Вне зависимости от размера, каждый блок имеет одну и ту же внутреннюю структуру. Данные растут с двух сторон навстречу друг другу, а между ними — динамическое свободное пространство:

```
+====================================================================+
|                    СТРАНИЦА (БЛОК) — 8 КБ (8192 байта)              |
|  0                                                          8191  |
+====================================================================+
|                            |                            |          |
|  PageHeader (24 байта)     |          >>> FREE <<<      |  Tuple 3 |
|  - lsn (позиция в WAL)    |          >>> SPACE <<<     |  ('Bob') |
|  - checksum                |                            |          |
|  - lower (конец ItemId)   |                            |  Tuple 2 |
|  - upper (начало кортежей)|                            | ('Alice')|
|  - special (для индексов) |                            |          |
+----------------------------+                            +----------+
|  ItemId [0] LP_UNUSED      |                            |  Tuple 1 |
|  ItemId [1] → Tuple1       |                            | (старая  |
|  ItemId [2] → Tuple2       |                РАСТЁТ      | версия)  |
|  ItemId [3] → Tuple3       |                 ВВЕРХ ↑    |          |
|          ...               |                            |          |
|    РАСТЁТ ВНИЗ ↓          |                            |          |
+====================================================================+
                             |                            |
                         lower                        upper
                         (граница ItemId)            (начало кортежей)
```

| Секция | Направление роста | Описание |
|:---|:---|:---|
| **Page Header** | Фиксирован (24 байта) | Метаданные блока: LSN, контрольная сумма, границы |
| **ItemIdData Array** | Вниз ↓ | Массив указателей (по 4 байта), каждый описывает один кортеж |
| **Free Space** | Динамическое | Пространство между указателями и кортежами |
| **Tuple Data** | Вверх ↑ | Сами данные строк — живые и мёртвые версии |

**Почему данные растут с двух сторон навстречу друг другу?**
- `ItemId` (указатели) растут вниз от заголовка.
- Сами кортежи (строки) растут вверх от конца страницы.
- Свободное пространство — это «буферная зона» между ними. Когда новый кортеж не влезает — он идёт на другую страницу, а эта помечается в FSM.

---

## 2.10 Заголовок страницы

```
+-----------------------------------------------------------+
|                  PAGE HEADER (24 байта)                     |
+-------+--------+-------+-------+-------+-------+-----+-----+
|  lsn  |checksum| flags | lower | upper |special|pages| ver |
|(8 байт|(2 байта|(2 байт|(2 байт|(2 байт|(2 байт|size |(2 б)|
|       |        |       |       |       |       |(2 б)|     |
+-------+--------+-------+-------+-------+-------+-----+-----+
|                  prune_xid (4 байта)                        |
+-----------------------------------------------------------+
```

| Поле | Размер | Назначение |
|:---|:---|:---|
| `lsn` | 8 байт | Смещение в WAL последнего изменения на этой странице. Критично для восстановления |
| `checksum` | 2 байта | Контрольная сумма для защиты от повреждений на диске |
| `flags` | 2 байта | Битовые флаги (например, `PD_PAGE_FULL`) |
| `lower` | 2 байта | Смещение до конца массива ItemId (граница «сверху») |
| `upper` | 2 байта | Смещение до начала самого нижнего кортежа (граница «снизу») |
| `special` | 2 байта | Смещение до специальной области. В странице таблицы пустой (0), в странице индекса — 16 байт метаданных (разберём в Главе 3) |
| `pagesize` | 2 байта | Размер блока (8000 для 8 КБ, 16384 для 16 КБ...) |
| `version` | 2 байта | Версия раскладки страницы |
| `prune_xid` | 4 байта | ID старейшей транзакции на момент последней очистки HOT |

---

## 2.11 Массив указателей ItemId

Каждый элемент массива — это 4 байта, работающие как «оглавление» страницы:

```
+-----------------------------------------------------------+
|              МАССИВ ItemId (по 4 байта каждый)             |
+-----------------------------------------------------------+
| [0]  | lp_off=0,lp_flags=LP_UNUSED,lp_len=0 | (свободен)  |
| [1]  | lp_off=7890,lp_flags=LP_NORMAL,lp_len=45 | 45 байт |
| [2]  | lp_off=7830,lp_flags=LP_NORMAL,lp_len=60 | 60 байт |
| [3]  | lp_off=7820,lp_flags=LP_DEAD,lp_len=10  | (мёртв)   |
| [4]  | lp_off=0,lp_flags=LP_REDIRECT,lp_len=0  | -> [2]    |
+-----------------------------------------------------------+
```

| Поле | Размер | Описание |
|:---|:---|:---|
| `lp_off` | 15 бит | Смещение кортежа от начала страницы |
| `lp_flags` | 2 бита | Состояние указателя |
| `lp_len` | 15 бит | Длина кортежа в байтах |

**Флаги `lp_flags`:**
- `LP_UNUSED` (0) — слот свободен, можно занять
- `LP_NORMAL` (1) — слот занят живым кортежем
- `LP_REDIRECT` (2) — HOT-редирект на другой слот (оптимизация обновлений)
- `LP_DEAD` (3) — кортеж мёртв, слот ждёт VACUUM

```
+-----------------------------------------------------------+
| TID (Tuple ID) = (block_number, item_pointer_offset)      |
|                                                           |
| Пример: TID (123, 5)                                      |
|   1. Идём в блок таблицы № 123                           |
|   2. Смотрим в массиве ItemId слот № 5                  |
|   3. Из lp_off узнаём смещение кортежа на странице       |
|   4. Читаем данные по этому смещению                     |
+-----------------------------------------------------------+
```

---

## 2.12 Кортеж: структура строки

Теперь рассмотрим саму строку — **кортеж**:

```
+====================================================================+
|                       КОРТЕЖ (СТРОКА)                               |
+====================================================================+
|                          |        |       |                |        |
| TupleHeader (23 байта)   | Null   | OID   | Данные         |Padding |
|                          | Bitmap |(4 б)  | столбцов       |(выравн)|
+====================================================================+
| <-------------- t_hoff (смещение до данных) --------------->        |
+====================================================================+
```

| Часть кортежа | Описание |
|:---|:---|
| **Tuple Header (23 байта)** | Фиксированный заголовок с метаданными для MVCC и доступа |
| **Null Bitmap** | Битовая карта NULL-значений. 1 бит на столбец. Присутствует, если есть NULLable-столбцы |
| **Object ID (OID)** | 4 байта. **Только если таблица создана с `WITH OIDS`** (сейчас не рекомендуется) |
| **Данные столбцов** | Значения полей в порядке создания столбцов, выровненные по границам |
| **Padding** | Дополнение для выравнивания общей длины кортежа (обычно кратно `MAXALIGN`) |

---

## 2.13 Заголовок кортежа

```
+-----------------------------------------------------------+
|        ЗАГОЛОВОК КОРТЕЖА (HeapTupleHeaderData, 23 байта)   |
+-------+-------+-------+-------+---------+---------+-------+
|t_xmin |t_xmax | t_cid |t_ctid |infomask2|infomask |t_hoff |
|(4 байт|(4 байт|(4 байт|(6 байт|(2 байта)|(2 байта)|(1 байт|
|       |       |       |       |         |         |       |
+-------+-------+-------+-------+---------+---------+-------+
```

| Поле | Размер | Назначение |
|:---|:---|:---|
| `t_xmin` | 4 байта | ID транзакции, создавшей эту версию строки |
| `t_xmax` | 4 байта | ID транзакции, удалившей/обновившей эту версию (0 = жива) |
| `t_cid` | 4 байта | Command ID — номер команды внутри транзакции-создателя |
| `t_ctid` | 6 байт | TID новой версии при UPDATE (или указывает на себя) |
| `t_infomask2` | 2 байта | Флаги: количество атрибутов в Null Bitmap, есть ли OID |
| `t_infomask` | 2 байта | Ключевые флаги видимости и состояния |
| `t_hoff` | 1 байт | Размер заголовка — смещение до начала данных |

**Флаги `t_infomask` (подсказки видимости):**

```
+-----------------------------------------------------------+
|    ФЛАГИ t_infomask (подсказки видимости)                 |
+-----------------------------------------------------------+
| HEAP_XMIN_COMMITTED  | xmin транзакция точно закоммичена  |
| HEAP_XMIN_INVALID    | xmin транзакция откатилась/сбой    |
| HEAP_XMAX_COMMITTED  | xmax транзакция точно закоммичена  |
| HEAP_XMAX_INVALID    | xmax транзакция откатилась         |
| HEAP_HASNULL         | в строке есть NULL-значения        |
| HEAP_HOT_UPDATED     | строка обновлена по HOT            |
| HEAP_ONLY_TUPLE      | строка создана как HOT-кортеж      |
+-----------------------------------------------------------+
```

Зачем нужны эти флаги? Без них, чтобы узнать статус транзакции `xmin`, нужно идти в системный каталог `pg_xact` (ранее `pg_clog`). С флагами статус «зашит» прямо в строку — это экономит огромное количество обращений.

---

## 2.14 Цепочка версий при UPDATE

Наш запрос `UPDATE users SET name = 'Alice' WHERE id = 1`. Вот что происходит на странице:

```
ДО UPDATE:
+----------------------------------------------------------------+
| Слот [1] LP_NORMAL                                              |
|   t_xmin=100, t_xmax=0, t_ctid=(0,1)                           |
|   name = 'Bob'                                                  |
+----------------------------------------------------------------+

ПОСЛЕ UPDATE (транзакция 200):
+----------------------------------------------------------------+
| Слот [1] LP_NORMAL                                              |
|   t_xmin=100, t_xmax=200, t_ctid=(0,2) ----→ указывает на слот 2|
|   name = 'Bob'                           |                      |
+----------------------------------------------------------------+
                                           |
+------------------------------------------|---------------------+
| Слот [2] LP_NORMAL (новая строка!)       ↓                     |
|   t_xmin=200, t_xmax=0, t_ctid=(0,2) — указывает на себя      |
|   name = 'Alice'                                               |
+----------------------------------------------------------------+

ЧТО ВИДЯТ ДРУГИЕ ТРАНЗАКЦИИ:
  • Транзакция, начавшаяся ДО транзакции 200:
    видит слот [1], name='Bob' (её снимок не включает изменения)

  • Транзакция, начавшаяся ПОСЛЕ COMMIT транзакции 200:
    видит t_xmax=200 у слота [1], проверяет что 200 закоммичена,
    идёт по t_ctid → слот [2] → name='Alice'
```

---

## 2.15 Свободное пространство и FSM

Когда на странице не хватает места для новой версии строки, она ищется в другой странице. Для быстрого поиска существует **Free Space Map (FSM)** — отдельные файлы с суффиксом `_fsm`:

```
+-----------------------------------------------------------+
| FREE SPACE MAP (FSM) - древовидная структура              |
|                                                           |
|                   [Корень: max free]                       |
|                  /                    \                    |
|        [Страницы 0-99]        [Страницы 100-199]          |
|         /        \               /          \             |
|    [0-49]      [50-99]      [100-149]    [150-199]       |
|                                                           |
| Каждый узел хранит МАКСИМАЛЬНЫЙ размер свободного         |
| куска в своей группе страниц.                             |
| Это позволяет БЫСТРО найти страницу с нужным местом.     |
+-----------------------------------------------------------+
```

FSM хранит в виде дерева информацию о примерном количестве свободного места на каждой странице, чтобы при вставке не сканировать всю таблицу.

**FSM — это не просто «файл на диске».** Когда PostgreSQL обновляет FSM (например, после вставки строки, которая заняла свободное место), это изменение:

1. Происходит в `shared_buffers` — страница FSM загружается в кэш, как и любая другая.
2. Логируется в WAL — как и любое другое изменение.
3. Позже сбрасывается на диск Background Writer'ом или Checkpointer'ом.

---

## 2.16 TOAST: когда строка не влезает

Что если наша таблица хранит огромный JSON или текст? Строка может быть больше 8 КБ! Если строка превышает примерно 2 КБ (четверть страницы), PostgreSQL сначала пытается сжать поля, а если даже после сжатия не влезает — выносит данные во внешнюю TOAST-таблицу.

```
ОСНОВНАЯ ТАБЛИЦА (users)
+-------------------+--------------------------------------------------+
| id | name | bio  |                                                  |
+----+------+------+                                                  |
| 1  | Alice| (TOAST-указатель)                                       |
|    |      |   тип: сжатый (PGLZ)                                   |
|    |      |   исходный размер: 15 КБ                                |
|    |      |   сжатый размер: 6 КБ                                  |
|    |      |   TOAST-таблица: pg_toast.pg_toast_16541                |
|    |      |   toast_id: 42                                          |
+----+------+---------------------------------------------------------+
            |
            | PostgreSQL автоматически создаёт TOAST-таблицу
            ▼
TOAST-ТАБЛИЦА (pg_toast.pg_toast_16541) — отдельные файлы на диске
+----------+-----------+----------------------------+
| chunk_id | chunk_seq | chunk_data                 |
+----------+-----------+----------------------------+
| 42       | 0         | [первые ~2 КБ сжатых данных]|
| 42       | 1         | [следующие ~2 КБ]           |
| 42       | 2         | [последние ~2 КБ]           |
+----------+-----------+----------------------------+
```

**Стратегии хранения TOAST (можно задать для столбца):**

| Стратегия | Поведение |
|:---|:---|
| `EXTENDED` (по умолчанию) | Сжать и вынести, если не влезает |
| `EXTERNAL` | Вынести БЕЗ сжатия. Быстрее для `substring()` |
| `MAIN` | Сжать, но оставить в основной таблице, если возможно |
| `PLAIN` | Запрет на TOAST и сжатие (для коротких данных) |

**Важно:** при `SELECT * FROM users` без обращения к TOAST-полю (например, только `id` и `name`) TOAST-таблица **не читается вообще**.

**TOAST-таблица — это обычная таблица.** У неё есть свои страницы, свои FSM, свои VM. Она точно так же кэшируется в `shared_buffers`, логируется в WAL и сбрасывается на диск. Для Buffer Manager'а нет разницы между обычной таблицей и TOAST-таблицей.

### 💡 Практика: как хранить большие значения

**✅ ОБЯЗАТЕЛЬНО ДЕЛАЙ:**

- **Знай: TOAST включается автоматически** при размере строки > ~2 КБ (четверть страницы). Ничего настраивать не нужно.

**👍 СТОИТ СДЕЛАТЬ:**

- **Используй `EXTERNAL` для данных, где нужен `substring()`:**
  ```sql
  ALTER TABLE users ALTER COLUMN bio SET STORAGE EXTERNAL;
  ```
  Подходит для: больших текстов, JSON, XML, где нужно читать часть значения без полной распаковки.

**🤔 НЕ ОБЯЗАТЕЛЬНО:**

- **Менять стратегию для обычных текстов.** `EXTENDED` (сжатие + вынос) хорошо работает по умолчанию.

**❌ НЕ ДЕЛАЙ:**

- **Не используй `PLAIN` для больших значений.** Строка не сможет быть вынесена в TOAST, вставка упадёт.

---

## 2.17 Аннотации сложности

| Операция | Time | Space | Use case |
|:---|:---|:---|:---|
| **Поиск страницы в файле** | O(1) — прямое вычисление смещения: `N * 8192` | 1 страница в `shared_buffers` | Buffer Manager: «дай страницу №100» |
| **Поиск строки по TID** | O(1) — прямое смещение в странице через ItemId | 1 страница в `shared_buffers` | После Index Scan: «дай мне блок 100, слот 5» |
| **Создание новой версии (UPDATE)** | O(1) — если есть место на странице, иначе поиск через FSM | Новый кортеж + запись в WAL | Каждый UPDATE/DELETE |
| **Чтение TOAST-значения** | O(N) — N чанков надо прочитать | Зависит от размера TOAST-объекта | `SELECT large_column FROM ...` |
| **Проверка видимости (MVCC)** | O(1) — после установки hint bits | Чтение флагов из заголовка кортежа | Каждое чтение строки |

---

## 2.18 Практика Go: pageinspect

Расширение `pageinspect` позволяет заглянуть внутрь страницы. Напишем Go-утилиту, которая показывает содержимое заголовка страницы.

```go
package main

import (
	"database/sql"
	"fmt"
	"log"
	"os"

	_ "github.com/jackc/pgx/v5/stdlib"
)

func main() {
	db, err := sql.Open("pgx", os.Getenv("DATABASE_URL"))
	if err != nil {
		log.Fatal(err)
	}
	defer db.Close()

	// Включаем pageinspect
	_, err = db.Exec("CREATE EXTENSION IF NOT EXISTS pageinspect")
	if err != nil {
		log.Fatal(err)
	}

	// Получаем информацию о страницах таблицы
	rows, err := db.Query(`
		SELECT 
			blkno,
			lower,
			upper,
			special,
			pagesize,
			version,
			prune_xid
		FROM page_header(get_raw_page('users', 0));
	`)
	if err != nil {
		log.Fatal(err)
	}
	defer rows.Close()

	fmt.Println("=== ЗАГОЛОВОК СТРАНИЦЫ 0 ТАБЛИЦЫ users ===")
	for rows.Next() {
		var blkno, lower, upper, special, pagesize, version int
		var pruneXid sql.NullInt64
		if err := rows.Scan(&blkno, &lower, &upper, &special, &pagesize, &version, &pruneXid); err != nil {
			log.Fatal(err)
		}
		fmt.Printf("Блок: %d\n", blkno)
		fmt.Printf("  Размер страницы: %d байт\n", pagesize)
		fmt.Printf("  lower (конец ItemId): %d\n", lower)
		fmt.Printf("  upper (начало кортежей): %d\n", upper)
		fmt.Printf("  Свободное место: %d байт\n", upper-lower)
		fmt.Printf("  special: %d\n", special)
		fmt.Printf("  version: %d\n", version)
	}

	// Получаем информацию о кортежах на странице
	rows2, err := db.Query(`
		SELECT 
			lp,
			lp_off,
			lp_flags,
			lp_len,
			t_xmin,
			t_xmax,
			t_ctid
		FROM heap_page_items(get_raw_page('users', 0))
		LIMIT 10;
	`)
	if err != nil {
		log.Fatal(err)
	}
	defer rows2.Close()

	fmt.Println("\n=== КОРТЕЖИ НА СТРАНИЦЕ 0 ===")
	for rows2.Next() {
		var lp, lpOff, lpFlags, lpLen int
		var tXmin, tXmax sql.NullInt64
		var tCtid string
		if err := rows2.Scan(&lp, &lpOff, &lpFlags, &lpLen, &tXmin, &tXmax, &tCtid); err != nil {
			log.Fatal(err)
		}
		fmt.Printf("Слот %d: offset=%d, flags=%d, len=%d, xmin=%v, xmax=%v, ctid=%s\n",
			lp, lpOff, lpFlags, lpLen, tXmin.Int64, tXmax.Int64, tCtid)
	}
}
```

**Что делает утилита:**
- Загружает `pageinspect` — расширение для исследования страниц.
- Читает заголовок страницы 0 таблицы `users`: размер, границы, свободное место.
- Читает первые 10 кортежей: их позиции, флаги, `t_xmin`/`t_xmax`/`t_ctid`.

**Запуск:**

```bash
export DATABASE_URL="postgres://alice@localhost/mydb"
go run page-inspect.go
```

**Пример вывода:**

```
=== ЗАГОЛОВОК СТРАНИЦЫ 0 ТАБЛИЦЫ users ===
Блок: 0
  Размер страницы: 8192 байт
  lower (конец ItemId): 240
  upper (начало кортежей): 7890
  Свободное место: 7650 байт
  special: 0
  version: 4

=== КОРТЕЖИ НА СТРАНИЦЕ 0 ===
Слот 1: offset=7890, flags=1, len=45, xmin=100, xmax=0, ctid=(0,1)
Слот 2: offset=7830, flags=1, len=60, xmin=200, xmax=0, ctid=(0,2)
Слот 3: offset=7820, flags=3, len=10, xmin=150, xmax=201, ctid=(0,4)
```

---

## 2.19 Выводы и типичные ошибки

**Что мы узнали?**

Дисковое пространство PostgreSQL организовано в строгую иерархию: кластер (директория `PGDATA`) → база данных (поддиректория в `base/` с OID) → схема (логическое пространство имён в `pg_namespace`, без физического представления) → таблица (файлы с `relfilenode` + `_fsm` + `_vm`) → страницы (блоки по 8 КБ, физически последовательно в файле) → кортежи (строки с заголовком `t_xmin`/`t_xmax`/`t_ctid` для MVCC). Buffer Manager находит страницу по смещению `N × 8192` в файле. Большие значения автоматически выносятся в TOAST-таблицу. Каждый UPDATE создаёт новую версию строки, не перезаписывая старую. `shared_buffers` кэширует **все** страницы: таблиц, индексов, FSM и VM.

**Типичные ошибки:**

- ❌ **Путать схему и базу данных.** Схема — логическое пространство имён, не создаёт директорий на диске. База данных — физически изолированный набор файлов. Таблицы из разных схем могут участвовать в одном JOIN, таблицы из разных баз — нет.
- ❌ **Думать, что размер строки не ограничен.** Даже с TOAST строка должна поместиться хотя бы в 1/4 страницы (~2 КБ для 8 КБ блока), иначе вставка невозможна. Для хранения очень больших значений используй `EXTERNAL`-стратегию TOAST или выноси данные в отдельные файлы.
- ❌ **Забывать про `t_ctid` при анализе производительности.** Если цепочка версий длинная (много UPDATE без VACUUM), чтение «свежей» версии будет прыгать по цепочке `t_ctid`, увеличивая задержку. Регулярный VACUUM сокращает цепочки.
- ❌ **Не учитывать стратегию TOAST.** `SELECT substring(bio, 1, 100)` при стратегии `EXTENDED` (по умолчанию) заставит PostgreSQL прочитать и распаковать **весь** TOAST-объект. Для таких запросов используй `EXTERNAL` (без сжатия) — тогда чтение части данных не требует полной распаковки.
- ❌ **Игнорировать FSM и VM.** Без FSM вставка новой строки будет сканировать всю таблицу в поисках места. Без актуальной VM Index Only Scan не сможет пропустить чтение таблицы, даже если все данные есть в индексе.
- ❌ **Менять `BLCKSZ` без понимания последствий.** Размер блока фиксируется при `initdb` для всего кластера. Для OLTP (много мелких записей) большой блок увеличит объём WAL и фрагментацию. Для OLAP (последовательное чтение) маленький блок увеличит накладные расходы.

---

## 2.20 Для быстрого повторения

- **Иерархия хранения:** Кластер (`PGDATA`) → База (поддиректория в `base/`, OID) → Схема (логическое пространство имён, `pg_namespace`) → Таблица (файлы: `relfilenode`, `_fsm`, `_vm`) → Страницы (8 КБ, физически последовательно) → Кортежи (строки с `t_xmin`/`t_xmax`/`t_ctid`).
- **Страница (8 КБ):** атомарная единица ввода-вывода. Buffer Manager вычисляет смещение в файле как `номер_блока × 8192`. Внутри: Page Header (24 байта) → ItemId (массив указателей) → Free Space → Tuple Data (кортежи растут вверх).
- **`shared_buffers` кэширует ВСЕ страницы:** таблиц, индексов, FSM, VM. Buffer Manager не различает их — для него это просто «страница в файле».
- **relfilenode:** имя файла таблицы на диске (обычно = OID, меняется при VACUUM FULL, CLUSTER, TRUNCATE). `_fsm` — карта свободного места, `_vm` — карта видимости (нужна для Index Only Scan).
- **Кортеж:** заголовок 23 байта: `t_xmin` (кто создал), `t_xmax` (кто удалил/обновил, 0 = жива), `t_ctid` (TID новой версии при UPDATE). Hint bits в `t_infomask` кэшируют статус транзакции.
- **TOAST:** автоматический вынос значений > ~2 КБ во внешнюю таблицу (`pg_toast`). Стратегии: `EXTENDED` (сжать и вынести), `EXTERNAL` (вынести без сжатия), `MAIN` (сжать, оставить в таблице), `PLAIN` (запрет TOAST). TOAST-таблица — обычная таблица со своими страницами, FSM, VM.
- **FSM (Free Space Map):** древовидная структура, быстрый поиск страницы с достаточным свободным местом для вставки. Без неё — полное сканирование таблицы.

---

## 2.21 Вопросы для самопроверки

1. Опиши полную иерархию хранения данных в PostgreSQL: от кластера до кортежа. Какие идентификаторы используются на каждом уровне?
2. Что такое `relfilenode` и чем он отличается от OID таблицы? В каких случаях `relfilenode` меняется?
3. Что хранится в файлах `_fsm` и `_vm`? Какую проблему решает каждый из них?
4. Как файл таблицы разбивается на страницы? Это физическое или логическое разбиение? Как Buffer Manager находит страницу по её номеру?
5. Какие страницы кэшируются в `shared_buffers`? Только страницы таблиц или что-то ещё?
6. Опиши внутреннюю структуру страницы. Почему данные растут с двух сторон навстречу друг другу?
7. Как устроен кортеж (строка) в PostgreSQL? Какие поля содержит заголовок кортежа и зачем нужно каждое из них?
8. Что такое hint bits (`t_infomask`) и зачем они нужны? Какую проблему они решают?
9. Как работает цепочка версий при UPDATE? Что происходит с `t_xmax` и `t_ctid` у старой и новой версии?
10. Что такое TOAST? В какой момент он срабатывает? Опиши четыре стратегии хранения TOAST и их компромиссы.
11. Ты нашёл на диске файлы `16385`, `16385_fsm`, `16385_vm` и `16385.1`. Что означает каждый из них?

---

## 2.22 Ответы

### Ответ 1

**Иерархия хранения (сверху вниз):**
1. **Кластер** — директория `PGDATA`. Содержит все базы данных одного экземпляра PostgreSQL.
2. **База данных** — поддиректория в `PGDATA/base/`, идентифицируется по OID (Object Identifier, 32-битное число). Полная изоляция: JOIN между базами невозможен.
3. **Схема** — логическое пространство имён, хранится в системной таблице `pg_namespace`. Не имеет физического представления на диске. `search_path` определяет порядок поиска таблиц по схемам.
4. **Таблица/Индекс** — идентифицируется OID в `pg_class`. Физически представлена файлами с `relfilenode` в качестве имени.
5. **Файлы** — основной файл (`relfilenode`), карта свободного места (`_fsm`), карта видимости (`_vm`). Каждый файл ≤ 1 ГБ; при превышении создаются `.1`, `.2` и т.д.
6. **Страницы** — блоки по 8 КБ (по умолчанию), физически последовательно в файле. Смещение страницы N = N × 8192 байт.
7. **Кортежи** — строки внутри страниц, заголовок 23 байта с полями `t_xmin`, `t_xmax`, `t_ctid`, `t_infomask`.

### Ответ 2

`relfilenode` (relation file node) — это **имя файла** таблицы или индекса на диске. Обычно `relfilenode` совпадает с OID таблицы, но **меняется** после операций, которые физически пересоздают файл: `VACUUM FULL`, `CLUSTER`, `TRUNCATE` (в некоторых случаях), `REINDEX`. После этих операций старый файл удаляется, создаётся новый с новым `relfilenode`. Это важно для инструментов, которые работают с файлами напрямую (бэкапы на уровне файловой системы), и для кэша планов запросов, которые инвалидируются при смене `relfilenode`.

### Ответ 3

**`_fsm` (Free Space Map):** хранит информацию о количестве свободного места на каждой странице таблицы. Организован как древовидная структура, где каждый узел хранит максимальный размер свободного куска в своей группе страниц. **Решает проблему поиска страницы для вставки:** без FSM пришлось бы сканировать всю таблицу в поисках места.

**`_vm` (Visibility Map):** битовая карта, где для каждой страницы отмечено два флага: all-visible (все строки видны всем транзакциям) и all-frozen (все строки заморожены). **Решает проблему Index Only Scan:** если страница помечена как all-visible, PostgreSQL может взять данные прямо из индекса, не читая таблицу. Обновляется VACUUM'ом.

### Ответ 4

Разбиение **физическое**: страницы идут последовательно в файле, каждая занимает ровно `BLCKSZ` байт (по умолчанию 8192), без зазоров. Buffer Manager находит страницу по формуле: `смещение = номер_страницы × размер_страницы`. Для чтения выполняет системный вызов `pread()` с вычисленным смещением. При росте таблицы новые страницы дописываются в конец файла. При превышении 1 ГБ создаётся новый файл с суффиксом `.1`, `.2` и т.д., при этом логическая нумерация страниц продолжается (страница 131072 — это страница 0 в файле `.1`).

### Ответ 5

`shared_buffers` кэширует **все** страницы, а не только страницы таблиц:
- Страницы таблиц (Heap Pages)
- Страницы индексов (B-tree, GIN, GiST, BRIN...)
- Страницы FSM (Free Space Map)
- Страницы VM (Visibility Map)

Buffer Manager не различает их: для него это просто «страница с номером X в файле Y». Всё, что читается с диска, проходит через `shared_buffers`.

### Ответ 6

Внутренняя структура страницы (8 КБ):
1. **Page Header** (24 байта) — LSN, контрольная сумма, границы `lower`/`upper`, `special`, `pagesize`, `version`, `prune_xid`.
2. **ItemIdData Array** — массив указателей (по 4 байта), растёт **вниз** от заголовка. Каждый элемент: `lp_off` (смещение кортежа), `lp_flags` (LP_UNUSED/NORMAL/REDIRECT/DEAD), `lp_len` (длина).
3. **Free Space** — динамическое пространство между массивом ItemId и кортежами.
4. **Tuple Data** — сами кортежи, растут **вверх** от конца страницы.

Данные растут с двух сторон навстречу, чтобы эффективно использовать пространство: не нужно резервировать место ни для указателей, ни для кортежей заранее. Когда новый кортеж не влезает в свободное пространство — он идёт на другую страницу.

### Ответ 7

Кортеж (строка) состоит из:
1. **TupleHeader** (23 байта): `t_xmin` (ID создавшей транзакции), `t_xmax` (ID удалившей/обновившей, 0 = жива), `t_cid` (номер команды внутри транзакции), `t_ctid` (TID новой версии при UPDATE), `t_infomask2` (число атрибутов, OID), `t_infomask` (флаги видимости), `t_hoff` (смещение до данных).
2. **Null Bitmap** — битовая карта NULL-значений (1 бит на столбец). Присутствует, если есть NULLable-столбцы.
3. **OID** (4 байта) — только если таблица создана с `WITH OIDS` (сейчас не рекомендуется).
4. **Данные столбцов** — значения в порядке создания столбцов, выровненные по границам.
5. **Padding** — выравнивание общей длины до `MAXALIGN`.

### Ответ 8

**Hint bits** — флаги в поле `t_infomask` заголовка кортежа: `HEAP_XMIN_COMMITTED`, `HEAP_XMIN_INVALID`, `HEAP_XMAX_COMMITTED`, `HEAP_XMAX_INVALID`, `HEAP_HASNULL`, `HEAP_HOT_UPDATED`, `HEAP_ONLY_TUPLE`. Они **кэшируют статус транзакции** прямо в строке. Без них каждый читатель должен был бы обращаться к системному каталогу `pg_xact` (ранее `pg_clog`), чтобы узнать, закоммичена ли транзакция `t_xmin`/`t_xmax`. Первый читатель, проверивший статус через `pg_xact`, устанавливает соответствующий hint bit, и все последующие читатели видят его без дополнительных обращений. Это критически важно для производительности Seq Scan больших таблиц.

### Ответ 9

При `UPDATE` (транзакция 200 обновляет строку, созданную транзакцией 100):
- **Старая версия:** `t_xmax` меняется с 0 на 200 (помечается как удалённая транзакцией 200), `t_ctid` меняется на TID новой версии (например, (0,2)).
- **Новая версия:** создаётся новый кортеж с `t_xmin = 200`, `t_xmax = 0`, `t_ctid` указывает на себя (та же страница и слот, где он лежит).
- **Цепочка:** старая версия → (`t_ctid`) → новая версия. Транзакции, начавшиеся до COMMIT транзакции 200, видят старую версию. Транзакции, начавшиеся после COMMIT, видят `t_xmax = 200` у старой версии, проверяют что 200 закоммичена, и идут по `t_ctid` к новой версии.

### Ответ 10

**TOAST (The Oversized-Attribute Storage Technique):** механизм автоматического выноса больших значений за пределы основной таблицы. Срабатывает, когда размер кортежа превышает `TOAST_TUPLE_THRESHOLD` (обычно ~2 КБ, четверть страницы). PostgreSQL сначала пытается сжать данные (алгоритм PGLZ), если даже после сжатия строка велика — выносит во внешнюю TOAST-таблицу (`pg_toast`).

**Стратегии:**
- `EXTENDED` (по умолчанию): сжатие + вынос. Компромисс: экономия места ценой CPU на сжатие/распаковку.
- `EXTERNAL`: вынос без сжатия. Быстрее для `substring()` и частичного чтения (не нужно распаковывать весь объект), но занимает больше места.
- `MAIN`: сжатие, вынос только в крайнем случае. Минимизирует обращения к TOAST-таблице, но основная таблица может раздуваться.
- `PLAIN`: запрет TOAST и сжатия. Только для коротких данных (иначе вставка невозможна).

### Ответ 11

- **`16385`** — основной файл таблицы с `relfilenode = 16385`. Содержит все страницы с данными. Если таблица > 1 ГБ, создаются дополнительные файлы.
- **`16385_fsm`** — Free Space Map. Хранит информацию о свободном месте на страницах для быстрого поиска при вставке.
- **`16385_vm`** — Visibility Map. Битовая карта видимости страниц, необходима для Index Only Scan.
- **`16385.1`** — дополнительный файл, созданный когда размер таблицы превысил 1 ГБ. Содержит продолжение страниц (логическая нумерация продолжается с 131072).

Все файлы находятся в директории базы данных (`PGDATA/base/{OID_базы}/`). Отсутствие `_fsm` или `_vm` означает, что соответствующий файл будет создан при первом VACUUM.

---

## 2.23 Куда идти дальше?

Мы разобрали полную иерархию дискового пространства — от кластера до кортежа внутри страницы. Теперь мы знаем, где физически лежат данные, как они организованы в страницы и как кортежи версионируются через `t_xmin`/`t_xmax`/`t_ctid`.

Но мы разобрали страницы **таблиц**. А что насчёт **индексов**? Индексы — это тоже файлы на диске, и они тоже состоят из страниц. Страницы индексов устроены **похоже** на страницы таблиц (тот же Page Header, ItemId, Free Space), но с одним ключевым отличием: в `special`-области хранится метаинформация для поиска по дереву.

- **Как PostgreSQL находит нужную строку среди миллионов за доли секунды?** Мы знаем, что Index Scan возвращает TID. Но как устроен сам индекс? → **Глава 3: Индексы B-tree — как PostgreSQL находит данные за миллисекунды.**
- **Что происходит со старыми версиями строк после многих UPDATE?** Мы увидели, что они остаются на страницах. Кто и когда их убирает? → **Глава 6: VACUUM и Autovacuum — как PostgreSQL борется с раздуванием.**
- **Как работает MVCC на уровне транзакций?** Мы знаем про `t_xmin`/`t_xmax`, но как именно определяется видимость строки для конкретной транзакции? Что такое снимки и горизонты? → **Глава 5: MVCC на глубоком уровне — снимки, горизонты и заморозка.**